apiVersion: 1

providers:
  - name: 'Graph & Entity Resolution Lab'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards
